# Dart's resolved dependency graph. Regenerated by `dart pub get`, and CI runs
# that step, so committing it would only be a file that goes stale without
# anyone reading it.
.dart_tool/
pubspec.lock
