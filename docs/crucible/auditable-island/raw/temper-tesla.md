I'll load the design-temper skill and the full strike bundle first, then hunt the six named flaws.The strike brief is a single-reviewer pass, not a four-family temper. I'll read the rest of the bundle and ground the claims in the repo.The live crucible dir is in the repo. I'll read DESIGN, FOLD, SPARK, and the rest of the truncated research.I need to ground the load-bearing claims in code and the app tab, not just the design prose.I have enough to strike: the signed-ack cap, the echo's missing client check, and the PII already named in deletion code.DISSOLVE

The candidate is slag as an auditable-island design. The forge correctly killed the slogan; it then kept a receipt-and-prober costume after removing the only property that made the costume work.

**1. §3 hollows out Tiers 2 and 3.** Completely. The operator is the named adversary. They never opt in, strip the sig, or sign only the traffic that cannot convict them. Absence “proves nothing” (`DESIGN.md §3`, `FOLD.md F-3`). The sole remaining adversary is an island that upgraded, kept signing, and then signed two different `msg_id`s for one `client_msg_id`. A hostile operator with the box will not do that. They will rewrite history, serve split views, or stay unsigned. Kelvin’s spark made the proof load-bearing for the next action (`SPARK.md`); Cast discarded that with the public log and kept the phantom anyway. A phantom that collects unsigned acks holds nothing. Tier 2/3 defend against a confused honest operator who incriminates himself. That is not Nick’s question.

The UniqueConstraint Fold leaned on (`FOLD.md F-4`) is not a security boundary on a box the operator owns. ISL-0002: FK off, they have the DB. They can `UPDATE`, delete+reinsert, or answer two sockets differently.

**2. Tier 1 is a real tautology and a fake position bind.** Write-path binding is real (`signing.py:297`). Read-path echo is missing (`message_view` `:48-70`). That much is measured.

The attack the app named is ULID relocation: same `channel`/`body`/`reply`, different row (`key-continuity/DESIGN.md` reason 2). Origin signs those content fields plus `client_msg_id`, **not** `msg_id` or `created_at` (`signing.py:197-217`). Echoing the row’s `client_msg_id` catches a sloppy transplant that forgets to copy the stored id. A motivated operator copies `client_msg_id` with the origin, drops the old row, inserts a new ULID. Uniqueness holds. Echo matches. Position moved. Undetected.

Two missing steps:

- **Client.** Today inbound `validateOrigin` passes `origin.client_msg_id` back in as the frame id (`origin_envelope.dart:243-251`, `message.dart:214-226`). A new island field that old clients ignore (silent-desync) does nothing until the app compares **outer** `client_msg_id` to `origin.client_msg_id` and fails closed on mismatch. The design never states that check as the product. Echo is not the capability.
- **Hostile operator.** The island also controls the echo. Consistent forgery is free. This is the same shape as the `/v1/keys` interim the app already refused to ship as security (`key-continuity/DESIGN.md`: island is liar and notary). Other readers never see the ack (sender-only). They cannot get island-independent position without a second client signature after ack, or a published receipt. Tier 1 does not close reason 2 against the stated threat. It closes honest-island bugs. Do not sell it as “worth doing even if everything else dies” under an auditability banner.

**3. C-5 is wrong, and the island already said so.** Official app: `newTempId: () => _uuid.v4()` (`chat_providers.dart:566`). Random per send, no timestamp. That is not the contract. The column is an unconstrained 64-char string. Deletion code, verbatim: `client_msg_id` “can hold an email/phone/handle. Naming a column `*_id` does not make attacker-controlled input non-PII” (`accounts_service.py:157-159`). Echoing that to every member, every read path, forever, broadcasts a field this repo already tombstones as PII.

Signed traffic already leaks it: `origin` is echoed and contains `client_msg_id`. Tier 1’s **new** leak is unsigned traffic, which Fold celebrated as a plus (`FOLD.md F-4`). That is enthusiasm smuggling. Cross-channel correlation is real for any client that reuses or structures ids (device prefix, counter, stable uuid). The friends ruling is slightly the wrong statute: this does not teach the *island* who is friends with whom. It publishes client-chosen metadata to peers. Same family as the CT-gossip deadlock you already refused to re-walk.

**4. Both. The cooling was honest; the leftover bet is nerve-loss.** Heat refuted “nothing left to be reputable about.” Design 06 already held the careful claim. Age-of-history died on the Ore falsifier (`DESIGN.md §6`). Then the author could not let a crucible end at a message to Andy plus a one-field tautology, so “sign the ack” stayed as “the actual proposal” after F-3 stripped the load-bearing property. C-4 still fires: zero convictions is a score; a new unsigned island whitewashes. Liability without compelled signatures is a guestbook.

**5. Tier 0 is a real finding in the wrong document.** `#2161` steps 3–4, Andy never saw Design 06 (`RESEARCH-crosstab.md` C8). Highest value per hour: yes. Ranking it as Tier 0 of an engineering design pads a hollow bet. Do it. It does not rescue Tiers 1–3.

**6. Fold also missed.** Ack is WS-only (`ws.py:225`); REST/bus-born get no receipt. Reactions have the same origin/cmid tautology and are unmentioned. Signed acks inherit `island_pubkey` TOFU: verify proves the manifest is self-consistent, “NOT … the island you meant to reach” (`island_identity.py:400-402`); the app deliberately does not verify the manifest it already fetches. Origin.cmid survives account deletion (origin is kept; row cmid is nulled). V-1 (body hash) is still open: an ack that does not cover body binds a position to a possibly different payload.

**Instead.** Post Design 06 to Andy. If the app still wants the tautology closed, file a small wire ticket: echo `client_msg_id` omit-when-absent, app fails closed on origin/row mismatch, constrain the field (high-entropy, no PII, no reuse), honest scope = honest-island bug, not operator defense, no ✓. Do not build signed acks or the prober. Long-run remains Design 06 Decision 3, gated on rotation, multi-device, and an explicit N=2 split-view admission (auditor, public anchor, or “we do not have this”).
