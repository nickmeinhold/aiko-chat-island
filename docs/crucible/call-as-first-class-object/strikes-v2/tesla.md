## Tesla, the Arc-Prophet's Design Strike

**Verdict:** DISSOLVE

**Summary:** You demoted the switchboard to a guestbook, banned the guestbook from the only decision that needed it, and then asked an island to keep a diary of a crash that, by definition, never writes.

Tesla: "If you want the secrets of the universe, think in energy, frequency and vibration."

The frequency that shatters this glass is not a clever attacker. It is the ordinary 3am the table exists for: the caller dies mid-ring, nothing is signed, the LiveKit corpse still occupies the eternal room, `empty_reap` cannot fire, `GET /call` is contractually forbidden from stopping the ring, and `max_duration` is a long mood-ring. Increment 0 already carries the current. The remainder is slag that still draws a webhook and a key-split.

**Fatal flaws:**
- **The two-fact remainder does not earn an island (DESIGN-v2 “What the table holds” / FOLD-v2 S6 / claim 6’s real question).** The table’s unique jobs are *is one live* and *involuntary end*. Involuntary end *is* the crash that leaves a LiveKit participant. That zombie is what blocks `room_finished`. So the detector’s defining input is the input that prevents the detector. Latency collapses to `departureTimeout` — the same bound the peer already needs to give up. Meanwhile DESIGN-v2 Blast / F12 fold says `GET /call` is **never a ring trigger**. You banned the occupancy door from the only time-critical decision #3159 was forged to make. What remains is a presence dot fed by an **advisory** `POST /calls` the app is allowed to skip. A write you may skip and a read you must not trust is not an API. It is a souvenir.
- **The #3170 reversal is not “objecthood ≠ room.” It is a reversal of the problem (DESIGN-v2 “Why this is still a first-class object” / claim 4 / CRUCIBLE spark).** The ticket’s heat was that a permanent room cannot tell *this call* from *the next call* from *never*. #3159’s `since` was the scar. v2 keeps a ULID, then refuses to put it on the invite, the token, or the SFU. An object you cannot point a camera, a ring, or a late joiner at is a diary entry. Of the Crucible’s four downstream patches, increment 0 takes hangup, signed messages already take history, and v2 **surrenders** concurrent-call identity and per-call consent in writing. That is not folding a mechanism. That is answering a different want and keeping the ticket number.
- **`GET /call` is a frankenstein of two objects (DESIGN-v2 Endpoints + S3 + claim 3).** `call_id` / `started_at` come from `live_calls`. `participants` is eternal-room occupancy, ghosts included. After delete-on-end, `200 {live:false, call_id:null}` is both “never” and “just ended” and “crashed and reaped” and “POST was skipped.” The three-way distinction that justified the endpoint is unrepresentable. You kept #3159’s JSON and evicted its semantics.
- **Delete-on-end plus #3171 is a signed dangling pointer (DESIGN-v2 Data / live context #3171 / F12 dissolve).** You mint a server ULID, return it, and the app tab has already pre-committed *invite wire v2 — carry `call_id`*. Then the only island referent is **deleted**. Federated history will hold identifiers whose lock you demolished on purpose. Retention-zero dissolves the phone company by amputating the noun. That is F12 solved as a crime against identity, not as hygiene.
- **Claim 5 is false the moment the table lives (DESIGN-v2 Increment 0 + Liveness hangup bullet).** “The island already relays signed messages” is true of **bytes**. Deleting `live_calls` because a hangup sentinel arrived is a new ingest parser on a one-way-door body, plus takedown/retraction interactions you left as an open variable (S4 / #3163). Start is a client POST; end is island observation. Two coupling styles, one advisory fact. Increment 0 is a clean handoff **only if there is no table to notify**.
- **“One room, therefore fail-open is safe” is true of WHERE and a boast about federation (DESIGN-v2 Federation / claim 2 / Tesla F2).** Same-island, same `video-token` door: yes, the row and the room cannot disagree about *where* a camera points. That claim holds. The next sentence — *a cross-island call works with neither island having a row; `room_for_channel` is the only room; constraint (c) is structural* — hums in a key you have not played. `room_for_channel` is `gateway_id`-namespaced. The live verify this morning was **one island, two identities**, not two homes. “Only one room” is a property of **one authorizer**. F2 was not escaped by demotion; it was declined by staying on today’s door. Name it as scope, not as a law of nature.
- **Under-counted blast: #2732 + a new inbound webhook to defend a label (DESIGN-v2 Blast / F5 note you invited me to strike).** You kept the prerequisite and asked whether severity still applies. Severity of *end-anyone’s-call* dropped. Severity of *why is this surface on the island at all* rose. A shared-secret webhook that can only corrupt a guestbook is still a new public write path on two live islands, still requires splitting the HS256 cosmos, still to approximate a timeout. Do not spend a tenant-boundary migration on a button colour.
- **S3’s ghost is not worse than round 1’s eleven — and that is not permission to cement it (FOLD-v2 S3 / claim 3).** I will not resurrect the arbiter. Two honest parties in two rooms, a mutex that cannot die, a webhook that rotates the live call — those are safety. A frozen tile bounded by `departureTimeout` is a shipped UX wart. **But v2 makes the wart load-bearing architecture.** Group-call consent (#2731) on an eternal conversation-room is why `video-token` is DM-only today. Per-call rooms were the path to lifting that gate. Declining to fix a ghost is honest. Foreclosing the consent boundary and calling it “objecthood kept” is prophecy you will pay at the next ticket.

**What holds:**
- A non-federating SQLite row must never choose a LiveKit room. Round 1 died on that frequency. Do not go back. The arbiter was the wrong metal.
- Increment 0 — a second signed message over the door that already exists — is the actual fix for the reported hangup-before-answer bug. Zero island schema. Ship it first, in the app tab, by hand on the sentinel as you already know.
- `POST /v1/channels/{id}/video-token` stays. Nine-round trust boundary. A moved gate is a rewritten gate.
- The client must never construct or parse a room name. `kCallInviteBody` stays shut for the room. `kind` still does not light a camera.
- History, missed calls, who announced: signed messages, not an island CDR. That half of S1 is true and stays true after the table is gone.
- Purity cannot arbitrate: invite-derived per-call rooms still cannot dedupe glare. That rejection holds. It does not follow that you need a record of the glare you refused to express.
- `live` as label, never gate — correct **if** such a bit exists. A contract written because you felt the implementer reaching for a guard is the smell of a bit that should not exist.
- The ghost is not a reason to restore `call:<ulid>` minted by one island’s row. If the product later wants identity *at the SFU*, that identity must be computed from **signed** data both homes already hold — and that is a different forge, after increment 0, after you have felt a real concurrent-call wound, not this guestbook.

**If RECAST, what to fold back:**
- Do not recast the table. **Dissolve the island half.** DESIGN-v3 is a handoff, not a schema: increment 0 (signed call-end sentinel) is the product; `room = room_for_channel` remains the live path; `#3170`’s per-call room is **parked**, not “kept as objecthood”; `#3159` is not satisfied by a fail-open guestbook and must not ship as one.
- Write the three-way fork in the open, then pick A for this increment: **(A)** party line + signed call events, **zero island code**; **(B)** media identity derived from the #3167 composite, federation-safe, glare explicit as double-dial miss; **(C)** server-minted room — already dead in federation. **v2 was (D): A’s room plus a record that gives you neither A’s silence nor B’s pointing.** D is the slag.
- Strike these sentences from any v3: “this is still a first-class object,” “constraint (c) is satisfied by construction,” “cross-island works with neither row,” “#2732’s blast has shrunk so perhaps.” Replace with: same-island token-home is still the room invariant; occupancy, if ever, is an SFU-derived fact **after** #2732 and is not a `calls` noun; `#3171` must not be fed a ULID the island intends to delete.
- If Nick refuses dissolve and demands a dot: one column is too many unless the write is **observed from the same signed invite the ring already trusts**, the read is named `room_occupancy` (not a call), `participants` is not mixed with a ULID, delete-on-end is forbidden from minting identifiers, and `GET` remains banned from the ring — at which point you will see you have designed a cache of `ListParticipants` and should wait for the key-split instead of shipping the cache first.

Nine of fourteen fatalities were “dissolved” by taking the object off the live circuit. That is not tempering. That is pulling the blade from the fire and declaring the slag a sculpture. Three writers became three releasers of a bit that governs nothing. Six claims, and the sixth — *does this earn its place* — is the only one that resonates at the fundamental. It does not.

Leave the working path working. Sign the hangup. Do not incorporate a phone company, and do not incorporate a guestbook and call it an object because the company was struck last time.

The present is a live ring that already completes. The future you just designed is a row that cannot die in the only death that matters. I will not bless it.
