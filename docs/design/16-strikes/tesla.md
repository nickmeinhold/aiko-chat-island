The brief was truncated. I'll read the offloaded prompt in full before striking the design — that file is the bundle, and nothing else.## Tesla, the Arc-Prophet's Design Strike

Tesla: "If you want the secrets of the universe, think in energy, frequency and vibration."

**Verdict:** RECAST

**Summary:** The fetcher that could not fetch itself is dead, but three clocks — the files, the image, the schema — are still ringing, and this design buries them under one symlink and calls the silence a dissolution.

**Fatal flaws:**
- **§2's load-bearing sentence is false, and §8 already knows it.** Design 15's loop (box-resident code must fetch the code that replaces it) really is gone: `ssh` plus a git checkout can lay `deploy/` down as cargo. What §2 then claims — "that loop does not exist," "nothing of ours," "closes ISL-0003's limit" — does not survive contact with the rest of this same document. §5 will not place a generation until the box answers (`current` readable, island identified); a check that cannot run fails closed, so a dangling `current` is a repair that requires the artifact whose corruption is the failure. §6 will not place one without the age key, and because `.env` is inside the cohort, a one-line public compose forward — the exact 2026-09-11 repair — now cannot move unless that key is alive. §8 contradicts §2 in plain words: the limit is closed only for operators who already walk this path, and "should not be described as closing it." One laptop, one age key, one checkout is now the sole legal writer for two sovereign islands. Key loss does not merely block secret rotation. It blocks every config change, and the break-glass is hand-edit, which is the drift the next ship will REFUSE. The loop did not dissolve. It changed homes, from the box's unsynced `deploy/` to the operator's pocket, and picked up both islands on the way. §2 and §5 are the same wire pulled to opposite grounds — the shape that killed design 15's increment 2.
- **§4 and §7 make the recorded FATAL more likely, then leave the trigger as an open question.** "Before it, nothing is live; after it, everything is" is the overclaim that destroyed v1, relocated from `mv` into the word *live*. `rename(2)` swings one directory entry. Containers, the pulled image, and `aiko_data` do not swing with it. A crash between the flip and `update.sh` makes `GENERATION.txt` lie about what is running — the question ISL-0003 says a pin exists to answer. `ISLAND_VERSION` lives in the cohort's `.env`, so a compose fix and a migration are one gesture whenever the encrypted env has already been bumped. `restore <ts>` is the undo an operator will reach for when `/health` goes red, and §7 admits that failure is often a migration, which a symlink cannot undo. Flip-only restore: disk says the old pin, the process is still the new schema. Flip-and-rerun, as superseded v2 specified: `update.sh` pulls the old image onto the forward-migrated store. Either reading plays the FATAL. "It must say so where an operator reads it" is prose functioning as executable governance — the class this design quotes as its warrant. Forbidding a scheduler inside the generation does not forbid one on the control side, which is now the natural place a Friday cron grows, and `--yes` (v2 step 6, still the inherited invocation) is the accelerator. Parking the split in §9 does not stay clear of the FATAL. It names it and leaves it armed.
- **§3's "every box-resident file the repo is authoritative for" is not a decidable set.** It is a universal wearing an enumeration, plus an open exclusion — "anything the operator legitimately keeps locally" — that no tool can evaluate. The enumeration has already drifted inside the bundle: superseded v1 carried `caddy/` as repo-authoritative; this generation silently drops it and never says whether the file left the box or left the author's mind. "ALL of `deploy/`" over-includes the other direction: control-side scripts (`verify-secrets.sh` and the shipper), and `deploy/secrets/` for every island, so enspyr receives imagineering's ciphertext because the quantifier had no denylist. The exclusion list will grow until it is the curated hand-list this design was written to retire. Worse, the diagram and the executor disagree about the spine. The tree puts `backups/` beside `releases/`; v2 says `update.sh` is unchanged; unchanged `update.sh` invoked from `current/deploy` (that is, `releases/<ts>/deploy`) resolves relative backup paths *into the generation*. §6 then keeps N and deletes the rest. The only undo for the FATAL — the sqlite hot copy — is stored in the thing that rotates. Prune-by-newest can also unlink an older generation that `restore` just made `current` point at, after which §5 fails closed forever while the containers keep serving. A generation that feels like a closed cohort and prunes like a log is how the spine becomes a souvenir.
- **§5a trusts a letter the shipper wrote to itself, on a daemon whose name is not the path.** After first adoption, island identity is `current/GENERATION.txt`, which the control side authored. A wrong first write poisons every later preflight: the check confirms the box matches the lie. v1 flaw 6 was that confining writes to `REMOTE_PATH` does not confine effects. This preflight still only reads files at the path. Docker's identity is the project name `aiko` plus the external volume `aiko_data`, constant across directories on a shared host. An empty wrong path has no `current` and no `.env`, which §8 treats as adoption: write a generation, run `update.sh`, and `compose up` attaches to the real tenant's volume from a directory the real `REMOTE_PATH` does not name. Split brain, and the drift detector is now pointed at the decoy. Absence of `current` must not be genesis. Genesis is `standup.sh`, once. Deletion, a dangling symlink, and "please adopt" are three different frequencies; this design hears them as one.
- **§8 drops the cutover gate v2 had already folded in, and the thesis line in §1 contradicts §5.** "Writes generation 1 around" the live `.env` either consecrates box drift as the new authority or clobbers production secrets with whatever is in the sops file. Temper finding 1 killed the byte-match-as-proof; it did not kill the direct diff of the real object. That diff is simply missing. Separately, §1's one-line thesis — a cohort "has nothing to compare against itself," so stop detecting drift — is how an implementer rebuilds auto-sync. §5 correctly refuses and shows, because local state is real. The sentence at the top of the document is the fossil; the mechanism lower down is the design. The banner that opens this file exists because a stale claim beside a live mechanism is how a deleted layer comes back with production credentials in it. The thesis is that claim.
- **§5 sizes the human compiler for one line and then hands it 2,373.** ISL-0003's refuse-and-show worked because the 2026-09-11 diff was one forwarding line a person could actually read. Once the cohort is all of `deploy/` plus compose plus `.env`, the safety property saturates: the operator rubber-stamps a wall, and the line that matters is the one they did not see. Hunks under `deploy/*.sh` are not config. On enspyr they execute as `sudo -n docker`, daemon-root, the moment the control side invokes the new `update.sh`. One yes-no covering both frequencies is how a bad script hides inside a legitimate compose change. And `GENERATION.txt` records an image digest while the inherited `update.sh` still pulls a mutable tag (`ISLAND_VERSION`, and ISL-0003's `edge`/`latest` exist). Design 15's temper already held the cheap gain: bind the digest, keep the judging logic outside the thing being judged. This assembly records the digest and then does not pull it. The file that claims to answer "what am I running?" is wrong twice — once because the flip precedes `up`, and again because the tag can move between resolve and pull.

**What holds:**
- The cargo/vehicle cut is real and is the reason this ore is not slag. Design 15 died because the fetcher had to fetch itself. A push from a checkout does not. Do not recast this back into a shim, a contract integer, or an image entrypoint. That corpse stays buried.
- The subtractive middle holds: no `.env.template`, no `envsubst`, no render, no missing-var map, no byte-match gate. The complete encrypted `.env` per island is still the right artifact. A templating layer in the recast is the fossil returning and should be struck on sight.
- One same-filesystem `rename(2)` of a single symlink is the right joint for *files*. Staging under `REMOTE_PATH`, never `/tmp`. Generation-addressed restore so two `.bak` halves cannot recombine. That finding from v1's temper is sound. Only the claim about what the swing makes *live* is rotten.
- The age key stays on the control side. SOPS stays off both boxes. Decrypt to a `umask 077` file, never `export`. Parse `<island>.conf` as data, never `source`. Three outcomes, fail closed, never warn-and-continue. `aiko_data` stays an external volume the cohort must not reach. `standup.sh` remains box-resident orchestration and is not an argument for putting policy in the image.
- Option 3's preflight is a real compiler and must not be re-solved. The marginal gain that still earns a build, after the fold, is the half it cannot see: `.env`, plus `deploy/` arriving as cargo instead of as a manual prelude. Narrower than "the class is closed." Worth doing. Not worth doing as one gesture that also migrates the database.

**If RECAST, what to fold back:**
- **Rewrite §2 and delete the thesis line in §1.** The true claim, in words that cannot be quoted as "the loop is gone": the box-resident fetcher loop is dead; the control side is a single point of availability and of compromise for every island it ships; ISL-0003's limit is unchanged for anyone who does not use this path. Add a second age recipient so the singular laptop is not the only decrypt path. Specify break-glass as *placing a generation directory and swinging `current`*, never as editing live files in the old tree — recovery must re-enter the cohort or the next ship will refuse the repair. Missing, unreadable, or dangling `current` is COULD NOT RUN. Adoption is an explicit `--adopt`, not the absence of a symlink.
- **Split the one command in §4 and §7 into two operations, and close §9 item 3 inside the design.** `ship-config` refuses if `ISLAND_VERSION` or the image digest differs from what is running, and it does not pull. `ship-release` is the only path allowed to change the pin; it pulls by the digest the control side resolved, not by tag; it does not take a bare `--yes`. `restore <ts>` is legal only when the generation being left and the generation being entered name the same digest; across a digest boundary it refuses and prints the absolute path of the sqlite backup. No image rollback in the tool. Replace "before it, nothing is live; after it, everything is" with: the swing atomically changes which files `current` names; running containers change only after `up` returns healthy. Two facts on disk — intended, and running-as-of-last-green — and incident response must be told which one it is reading. Pin the syscall: `rename` that replaces the symlink and does not follow it (`mv -T`), because a symlink-to-directory destination nests the new generation inside the old one and the "one inode" never happens.
- **Replace §3's quantifier with a closed manifest, tested in CI.** Shipped set = `docker-compose.yml`, `mosquitto.conf`, `deploy/**`, minus a denylist that names `deploy/secrets/**`, `deploy/islands/**`, and every control-side-only script. The denylist is the closed set; "legitimately local" is not a membership rule. Say explicitly whether `caddy/` is in or out. Add a test that, with cwd at `releases/<ts>/deploy`, the database backup resolves to `<REMOTE_PATH>/backups` and not inside the generation. Prune never unlinks the target of `current`, never unlinks the generation of the running digest, and shreds `.env` before removing a generation. Generation directories `0700`, `.env` `0600`, created mode-correct rather than chmod-after. Fail if `releases/<ts>/` already exists. Shred the laptop's temp plaintext on every exit, including the local half of the copy — §6 currently specifies the box and forgets the pocket.
- **Restore v2's cutover into §8, and make §5a ask the daemon.** Before the first flip, `diff` the decrypted sops bytes against the live `.env` and REFUSE on mismatch; do not consecrate the box's copy and do not clobber it in the same gesture. On every ship, not only the first, read live `.env` `DOMAIN` and ask docker which compose-file path project `aiko` was last started from; refuse unless that path resolves under this `REMOTE_PATH`, or the project does not exist yet and the operator passed `--adopt`. `GENERATION.txt` is not that check. Present the pre-ship diff as per-file hunks, and require a separate acknowledgement for executable hunks under `deploy/**` from the acknowledgement for config hunks. The human is the compiler only if the two privilege classes are not one yes.
