The brief is truncated, so I’ll read the offloaded prompt first and judge the four sentences against the full bundle.The pin did leave the shipped file — that measurement holds. The first-ship gate and the cutover that creates it are still two waves on the same line, out of phase.

## Tesla, the Arc-Prophet's Design Strike

**Verdict:** RECAST

**Summary:** The pin left the cohort for a file the shipper does not write — and the first ship is now refused by the one-line difference that leaving it requires, while an empty `pin.env` still interpolates `edge` and tracks `main`.

Tesla: "If you want the secrets of the universe, think in energy, frequency and vibration."

**Did the author write your four sentences?:**
- **KEYLESS — YES.** Deleted, not replaced. Nick's ruling is the ground. The second age recipient is runbook advice. §1 is no longer inverted to ship a void.
- **INVOCATION — YES.** §4a names `--project-directory`, `-f`, both `--env-file` flags, and the inode assertion (canonical parent of the written inode, not a `printf`). The word **"unchanged" still sits in §2** and will reinject the round-3 shape if an implementer reads the earlier paragraph as law.
- **PIN — PARTIAL.** You were told to stop *saying* the pin left and to restore one comparison. You did the harder thing: you *measured* a leave (`--env-file` twice, both live Compose versions, merge is real) and put `ISLAND_VERSION` in operator-owned `pin.env`. That half is the sentence round 3 would have accepted as truth. The other half — *"no gate, because the shipper cannot move a pin it does not write"* — **fails the test this round exists to run.**
- **FIRST SHIP — YES, as prose; NO, as a reachable state.** §5b names the state, gates it on the §7 cutover diff, and fails closed on a real-directory `mv -T` instead of falling through to plain `mv`. Then §3d, which must run *before* that first ship, makes the two sides of that diff unequal by exactly the pin line.

**New or surviving fatal flaws:**
- **§3d and §5b are the same gate, inverted. Genesis Barrier, reconstituted.** Cutover order is: write `pin.env`, strip `ISLAND_VERSION` from sops, rewrite the invocation — *before* the first ship. After that strip, decrypted sops and the live `.env` (measured: still at `.env:12` / `.env:14`) differ by one line. §5b/§7: first ship is that diff, **REFUSE on mismatch**. So the required preparation is the refuse condition. Mid-cutover, "done," and "box drift" are the same byte predicate. The tool cannot distinguish them, and it cannot adopt either live box without a human smuggling the pin line past a compiler whose job is to refuse smuggling. Wrong order on the second box is the same trap per island, not a cross-box special: sops is per-island, `update.sh` is per-box, and an interrupted box 2 with sops already stripped will not ship until someone deletes the live pin — which, if the invocation is not yet the §4a line, is the unpin. Class: coupling / n=0. The first ship was named and then poisoned by the paragraph that made the pin's leave legal.
- **Empty `pin.env` is R3-1's second horn, not closed.** Missing `--env-file` target: Compose fails closed (the kind default). Empty file, or a file with no `ISLAND_VERSION` key: `--env-file` succeeds, interpolation hits `${ISLAND_VERSION:-edge}`, **`edge` tracks `main`.** Before this round that default was dead code on a box whose `.env` always carried the pin. Round 4 made it the live fallback of a **fourth box-resident file** that nothing ships, nothing hashes, nothing preflights, and nothing checks for a key. The shipper indeed does not write `pin.env`. It does not have to. Unset is enough. Two islands, 3am, a truncated write or a `touch` during cutover, next `update.sh` pulls `main`, schema *N+1*, recorded FATAL, sqlite path unprinted because the boundary was declared uncrossable. Nick's ruling gives the operator the pin; it does not license a default that *unpins*. ISL-0003's sentence was "a known version, not whatever `main` was when I last pulled." Class: under-counted blast-radius.
- **Half-applied compares two names of the same live symlink.** §4a passes `--project-directory "$REMOTE_PATH/current"`. `current` is a symlink the swing retargets. §5 then asks whether canonical `working_dir` ≠ `realpath(current)`. If the daemon recorded the path you passed, realpath-at-inspect follows the swung symlink and **equals** `realpath(current)` — half-applied is invisible, including across `restart: unless-stopped`, which is the reboot gap this predicate was written to catch. If you skip realpath on the label, `$REMOTE_PATH/current` ≠ `$REMOTE_PATH/releases/<ts>` **always**, and every healthy box looks half-applied until the alarm is ignored. One comparison, two implementations, both wrong unless you have *measured* that Compose stores EvalSymlinks-at-`up` (the release inode, not `current`). The design specifies the invocation that feeds the symlink and the discriminator that realpaths it, and does not pin which fact the label holds. Class: unstated assumption. Incident response is again not told which of the two disk facts it is reading.

**What holds:**
- Repeated `--env-file` merge is a fact on both live boxes (2.40.3 and v5.1.0). The pin *can* live outside the generation with no templating, no export, and no second ship command.
- `restore` swings generations; `pin.env` is not in one. The digest gate stays gone for a reason that holds. One command stays one.
- Keyless is a corpse. §1's pair is intact. No ninth ship path.
- Three enumerated files, no executables, denylist buried, `local/` dead, staging under `REMOTE_PATH`, `mv -T` symlink-onto-symlink, plain `mv` forbidden, inode assertion on backups, `standup.sh` the only genesis, no scheduler, no pull, no recreate in the shipper. The cargo/vehicle cut did not grow a component; it grew a flag.
- Kelvin's Ghost is closed for *variables* in `image:` (CI grep). A literal tag in a shipped compose is still a human-compiler line, not a second interpolation hole.
- `mosquitto.conf` still earns its seat. Preflight is still a real compiler and still must not be re-solved.

**If not SOUND, what to fold back:**
- **Make the first ship and the pin-lift the same compiler, not two.** Either: the §7/§5b diff permits *exactly one* difference, the `ISLAND_VERSION` line (sops already lifted, live `.env` not yet), and refuses on any other hunk — or: §3d is four steps in this order, each fail-closed: (1) write `pin.env` from the box's current pin, (2) install the §4a invocation, (3) verify with **that** invocation that `docker compose config` resolves the **same** image tag as before, (4) *then* remove the line from live `.env` and from sops, after which the byte-diff is equal and the first ship can proceed. The present three-step order cannot reach a state that is both pinned and shippable. Prose that "the tool refuses rather than assuming" is not a predicate until those four facts have a home.
- **One comparison on the pin, pointed at the file that holds it.** `update.sh` refuses unless `pin.env` sets a non-empty `ISLAND_VERSION` that is not the `edge` default — or `:-edge` is deleted so unset is a hard interpolation error. Missing file is already closed. Empty file is not. This is the gate from round 3, not a new machine, and it is why "the shipper does not write `pin.env`" is not enough. Do **not** restore a restore-digest gate; that path is actually dead.
- **Pin the half-applied discriminator to an inode that does not follow the live symlink.** Measure on both boxes what `com.docker.compose.project.working_dir` actually records under the §4a invocation. If it is `.../current`, this predicate cannot work — use a daemon fact frozen at `up` (release path after EvalSymlinks at create time, or the compose bytes the container was started from). Write the measured fact. Do not leave "canonical" to do the work.
- **Delete "unchanged" from §2.** The invocation is the delta. A fossil word here reopens R3-3 without another finding.

Three, six, nine: three files still ship; six places a pin can sit during cutover (sops, live `.env`, `pin.env`, generation `.env`, the process environment, the compose default); nine is `edge`, the frequency that tracks `main`. The leave is real. The default is the blast. Write the two predicates so they agree on the first ship, fail closed on an empty pin, and name an inode the swing cannot retarget — and then, this time, **stop.**
