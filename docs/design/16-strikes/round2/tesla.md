The brief was truncated mid-strike. I'll read the offloaded prompt in full before judging the fold — nothing else on disk.The fold is in hand. Two load-bearing claims — what `mv -T` does to a symlink, and what the daemon still knows after the project is stopped — get checked before they go into the strike.## Tesla, the Arc-Prophet's Design Strike

**Verdict:** RECAST

**Summary:** Tesla: "If you want the secrets of the universe, think in energy, frequency and vibration." — three commands, three states, and three clocks are still one `.env`, the swing names a flag whose literal operands error, and the tenant check goes silent the moment the containers stop, which is when the volume is still there.

**Did the fold discharge round 1?:**
- **Backup inside the pruned generation (§3b, flaw 1) — PARTIAL.** The absolute home and the prune exclusions are the right sentence. The gate that was the condition of discharge cannot go red on the write that fails: it asserts a resolved string at a synthetic cwd, and `printf '%s/backups' "$REMOTE_PATH"` satisfies it without reading that cwd, without executing `update.sh`, and without an inode.
- **Every island's ciphertext (§3, flaw 2) — PARTIAL.** Today's known paths are denied and `caddy/` is decided. `INCLUDE deploy/**` minus a denylist still defaults the next path to cargo, the shipper is still a pronoun, and a CI assertion of the deny-list locks the exceptions rather than the tree.
- **§2's false dissolution (flaw 3) — DISCHARGED.** The thesis line is dead, the true claim cannot be quoted as "the loop is gone," break-glass is a swing, and compromise stays named. The second age recipient covers availability only — a second decryptor is a second door, and §9.1 is right that compromise is untouched.
- **The FATAL left armed, one command, `mv`, digest (flaw 4) — PARTIAL.** Two commands, digest-pinned pull, digest-gated `restore`, no image rollback, no scheduler, and an honest sentence about what the swing makes live. Surviving: `mv -T` is pinned as a flag without operands, and `ship-config`'s refusal has no legal sibling when the pin has already moved.
- **Self-letter identity and the daemon path (§5c, flaw 6) — PARTIAL.** Absence is no longer genesis, `--adopt` is explicit, and a wrong `GENERATION.txt` is no longer the whole identity. The question the section asks the daemon is one the daemon answers with container labels — absent from every default query once the project is stopped, and absent from every query once it has been `down`'d.
- **Human compiler, one yes for two privilege classes (§5a, flaw 7) — DISCHARGED.** Executable hunks under `deploy/**` and config hunks are not one acknowledgement. A class-level second yes is still a reflex, not a reading of 2,373 lines; that limit is real and it is not this finding. Do not grow a third prompt and call it a gate.
- **Cutover consecrates or clobbers (§8, flaw 9) — DISCHARGED.** Decrypted sops bytes against the live `.env`, refuse on mismatch, reconcile, then adopt.
- **Hygiene (flaw 10) — DISCHARGED.** `0700`/`0600` at creation, laptop plaintext shredded, `releases/<ts>/` failing closed if it exists, retention capped.

**New or surviving fatal flaws:**
- **§3b — the backup gate cannot fail the way the bytes fail.** The undo for the recorded FATAL is written by inherited `update.sh` via Python `.backup()` inside the container (ISL-0003), onto a bind whose host side Compose resolves from the **canonical** project directory. `current` is a symlink; the daemon's project directory is `releases/<ts>`. The same relative arithmetic that is correct from logical `current/deploy` (`../../backups` → `<REMOTE_PATH>/backups`) is one directory short from the physical cwd and lands in the generation — the directory retention then prunes. The specified test sets that physical cwd and then accepts an absolute string. It never builds the symlink, never asks the daemon for the canonical project directory, and never stats the inode that received the file. CI is also the place where that daemon is not. A gate that runs where the failure cannot occur, and that a constant can satisfy, is prose wearing a test harness. At 3am the green check and the shredded sqlite are the same generation.
- **§4c — `mv -T` is pinned as a flag, and the operands that make it a swing are missing.** The diagram wants `current` to remain a symlink into `releases/`. The operation that does that is `ln -s releases/<ts> current.next` and then `mv -T current.next current`: symlink onto symlink, one `rename(2)`. The operation an implementer writes from the words "swung with `mv -T`" is `mv -T releases/<ts> current`. The source is a directory; the destination is a symlink, which is not a directory; GNU `mv` refuses with `cannot overwrite non-directory with directory`. The swing, as the nouns in §4c name it, does not succeed. The 3am repair of that error is dropping `-T` — and a plain `mv` of a directory onto a symlink-to-directory nests the new generation inside the old one, which is the bug the flag was added to kill. Every later invariant (prune the target of `current`, backup arithmetic, "under `releases/`") assumes a shape the literal syscall never produces.
- **§5c — a stopped project and a missing project are the same signal, and the signal is `--adopt`.** "Which compose-file path was project `aiko` last started from" is not a project record. It is two container labels, `com.docker.compose.project.working_dir` and `com.docker.compose.project.config_files`, and the second is often the relative string `docker-compose.yml`. `docker compose ls` and `docker ps` list **running** projects by default; stopped containers appear only with `--all`. So the default answer for a stopped island is empty. `docker compose down` removes the containers and therefore the labels, and does **not** remove an external volume. `aiko_data` is the tenant, and it is the thing that survives. §5c defines the empty answer as "the project does not exist," and that branch plus `--adopt` is allowed to `compose up`. Adopt of a decoy `REMOTE_PATH` onto a surviving `aiko_data` is the split brain flaw 6 existed to close, entered through maintenance instead of through a missing `current`. An inspect error is not classified as COULD NOT RUN, so a daemon failure types as the same empty. The design asks the fact the daemon forgets and ignores the fact it keeps.
- **§4a and §5 — the split re-couples through the one file, and the half-swing is typed as drift.** `ISLAND_VERSION` lives in the cohort `.env`. `ship-config` refuses when that pin or its digest differs from what is running, and it never pulls. `ship-release` is the only command that may move files anyway, and it is the command that pulls and can migrate. So the ordinary repair — a compose forward, a secret rotation, the 2026-09-11 line — is legal only while the encrypted env's pin already matches the running image. The moment git is ahead (the normal state between bump and release), or the box has drifted, or the island tracks `edge` and the tag's digest has moved, the safe door is shut and the dangerous door is the only door. There is no specified command that places a cohort while **holding** the running digest. Separating the commands without separating the pin from the secret blob is two labels on one wire. §5 makes the crash window worse: **baseline** is "what the operator believes the box is **running**," **live** is "what ssh reads from `current`." §4 correctly says those diverge when the swing landed and `up` did not. `live != baseline` then REFUSES, and the repair of a half-applied ship is classified as drift. Three states were named; the equality compares a process-belief to a directory.
- **§3 — a denylist is closed only for the paths already imagined.** The rule that decides the next ambiguous directory is not written, and §9.2 admits it. `INCLUDE deploy/**` means the next directory is cargo until a human remembers to deny it. CI that asserts the denylist stays green when `deploy/keys/`, a second sops tree, or the shipper's real path (the entry is still the pronoun `<the shipper itself>`) appears. The first miss is replicated to every island, retained for N generations, and shred is specified only for `.env`. I called the denylist "the closed set" in round 1. That phrase is the fossil. A denylist closes the present enumeration and opens the future; the blast is the first path nobody has classified yet.
- **§3c — `local/` is the bypass the refusal teaches.** The disposition was a directory **or** a hash. The fold does both. The hash watches `current/`; `local/` sits outside it and survives every flip, and the section's reason for existing is the 3am edit to `current/.env`. That edit is now REFUSED — correct — and the operator is shown a directory the hash does not cover, whose membership rule is "box-local state," the phrase §3 forbids as a membership rule. The first time a shipped compose file or `update.sh` reads it (`env_file`, a bind, a source), effective config is cohort ⊕ `local/`, the drift predicate stays green, and "the artifact in git IS the artifact on the box" is false again. Nothing in CI forbids the reference. Separately, "a generation records a manifest hash" stores the hash in the only record the section has, which is inside the tree being hashed. `GENERATION.txt` as an input to its own hash is either always dirty — and the check gets deleted — or excluded by an implementer with no spec for what else the running process may write.

**What holds:**
- The cargo/vehicle cut. The box no longer fetches its own replacement. Do not recast this back into a shim, a contract integer, or an image entrypoint.
- The subtractive middle. No template, no `envsubst`, no render, no byte-match-as-proof. A templating layer in the next fold is the fossil returning.
- One same-filesystem `rename(2)` is still the right joint for **files**, staging under `REMOTE_PATH`, never `/tmp`. Only the operands of that rename are wrong.
- Secret posture: decrypt to a `umask 077` file, never `export`; `<island>.conf` as data, never `source`; shred the pocket as well as the box; modes at creation.
- `ship-release` pulls a digest the control side resolved. `restore` across a digest boundary refuses and prints the sqlite path. No image rollback in the tool. No scheduler. The shipper does not initiate.
- Cutover refuses a mismatched `.env` instead of consecrating it. Three outcomes, fail closed. `aiko_data` stays an external volume the cohort must not contain. `standup.sh` stays box-resident orchestration. Option 3's preflight is a real compiler and is not re-solved.
- §2's claim is finally the size of the mechanism. That honesty is load-bearing. Do not decorate it back into "the loop is gone."

**If RECAST, what to fold back:**
- **§3b.** The gate invokes the backup logic that actually ships, from both `current/deploy` and the physical `releases/<ts>/deploy`, and asserts the **canonical parent of the written inode** is `<REMOTE_PATH>/backups`. A string helper must not be able to pass it. Relative binds in the shipped compose file are part of the same assertion, because the daemon canonicalizes the symlink before the container writes.
- **§4c.** Write the two commands: `ln -s releases/<ts> current.next` then `mv -T current.next current`, both under `REMOTE_PATH`. State that `mv -T` of the generation directory onto `current` is a specified failure, not an implementation choice. Fail closed if `mv` has no `-T`; do not fall back to plain `mv`.
- **§5c.** Three daemon answers, not two. Labels present and the joined `working_dir` + `config_files` path under this `REMOTE_PATH`: proceed. Labels present and the path elsewhere: REFUSE. No containers: do not call this absence. If the external volume `aiko_data` exists, this is a stopped tenant — REFUSE `--adopt`. Inspect errors are COULD NOT RUN. Genesis remains `standup.sh`. Query `ps -a` / the labels, not default `docker compose ls`, which hides stopped projects.
- **§4a / §5.** Take `ISLAND_VERSION` out of the blob `ship-config` replaces, or have `ship-config` rewrite the placed pin to the **running** digest, show that override, and refuse to `up` any other image. Name the half-swing as its own state, with continue-or-restore, not as drift. Baseline and live are both file facts; "what is running" is a daemon fact and is not the same comparison.
- **§3.** Replace `deploy/**` minus a denylist with an allowlist. CI lists the tree that would ship and fails on any unclassified path. The rule for the next directory is: unclassified is a red build, zero islands touched. Name the shipper by path. Shred every secret-shaped file the cohort could have carried, not only `.env`.
- **§3c.** Delete the overlay. One mechanism: the manifest hash REFUSES a modified `current/`. Nothing in the shipped manifest may reference `local/`, and CI greps for that reference. Record the hash outside the hashed bytes. If `local/` stays as scratch, it is not config and the cohort cannot see it.
- **Constraint, not a seventh subsystem.** §2's provenance pin must refuse a dirty tree and stamp the tree hash of the shipped manifest, not only an ancestor sha — a one-line correction, not a new component. §9.6 is already groaning: this paper is past "a small control-side shipper plus restore." The next fold gets **smaller**. If these six corrections cannot be specified without new machinery, round 3 is option 3 — one manual `deploy/` sync per box, plus the preflight that already exists — and not another layer.
